pytesseract>=0.3.10
Pillow>=9.0.0
requests>=2.31.0
numpy>=1.24.0
gpiozero>=2.0
lgpio>=0.2.2.0

# Install picamera2 from apt on Raspberry Pi OS:
#   sudo apt install -y python3-picamera2 python3-libcamera
# NeoPixel support is optional and not enabled by default on Pi 5.
